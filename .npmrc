# Usar cache agressivamente
prefer-offline=true
# Não buscar metadados desnecessários
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
# Evitar uso excessivo de recursos
loglevel=error 